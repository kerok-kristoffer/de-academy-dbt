version: 2

models:
  - name: work_product_transform
    config:
      database: SCD2_DB
      schema: SILVER
      materialized: table

sources:
    - name: source
      database: SCD2_DB
      schema: BRONZE
      tables:
        - name: WORK_PRODUCT_COPY

    - name: xfm
      database: SCD2_DB
      schema: SILVER
      tables:
        - name: WORK_PRODUCT_TRANSFORM